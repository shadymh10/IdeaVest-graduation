apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: coolad-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/YOUR_USERNAME/coolad-cd.git
    targetRevision: main
    path: k8s/overlays/dev
    kustomize: {}

  destination:
    server: https://kubernetes.default.svc
    namespace: coolad-app

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
